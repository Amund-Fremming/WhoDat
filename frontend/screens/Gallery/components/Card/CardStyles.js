import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        width: 10,
        height: 10,
        justifyContent: "center",
        alignItems: "center",
    },
});
